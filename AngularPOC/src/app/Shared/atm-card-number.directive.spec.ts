import { AtmCardNumberDirective } from './atm-card-number.directive';

describe('AtmCardNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new AtmCardNumberDirective();
    expect(directive).toBeTruthy();
  });
});
